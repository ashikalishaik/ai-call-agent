version: '3.8'

services:
  ai-call-agent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./app.py:/app/app.py
    environment:
      - HOST=0.0.0.0
      - PORT=8000
